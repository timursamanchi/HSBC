pipeline {
    agent any
    environment {
        MAX_SIZE = 100
        MIN_SIZE = 10
    }
    Stages {
        stage ('Checkout SCM') {
            steps {

            }
        }
        stage ('initialise build') {

        }
        
    }
}